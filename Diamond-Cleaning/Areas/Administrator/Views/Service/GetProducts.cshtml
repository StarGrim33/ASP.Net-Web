@{
    int i = 1;
}
@model List<Service>;

<div class="row">
    @await Html.PartialAsync("MenuAdministrator")

    <div class="col-9">
        <h3 class="text-center">Продукты</h3>
        <br />
        <a class="btn btn-outline-warning" asp-area="Administrator" asp-controller="Service" asp-action="Add"><i class="bi bi-database-add"> Добавить новый продукт </i></a>
        <div class="row">
            <div class="col">
                <table class="table">
                    <thead>
                        <tr>
                            <th>№</th>
                            <th>Название</th>
                            <th></th>
                            <th>Цена</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var product in Model)
                        {
                            <tr>
                                <td> @i </td>
                                <td class="col-3"><a style="color:navy" asp-area="Administrator" asp-controller="Service" asp-action="index" asp-route-id="@product.Id"><p>@product.Name</p></a></td>
                                <td><div class="col-2 text-start"><img class="img-fluid" src="@product.Link"></div></td>
                                <td class="col-2">@string.Format("{0:### ### ###}", product.Cost) р</td>
                                <td class="col-4 text-end">
                                    <a class="btn btn-outline-warning" asp-area="Administrator" asp-controller="Service" asp-action="edit" asp-route-id="@product.Id"><i class="bi bi-pencil"> Редактировать </i></a>
                                    <a class="btn btn-warning" asp-area="Administrator" asp-controller="Service" asp-action="delete" asp-route-id="@product.Id"> <i class="bi bi-trash-fill"> Удалить </i> </a>
                                </td>
                            </tr>
                            i += 1;
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>